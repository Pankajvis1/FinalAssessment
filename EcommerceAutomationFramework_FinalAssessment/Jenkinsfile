pipeline {
    agent any

    tools {
        maven 'Maven3'
        jdk 'jdk 17'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'master',
                    url: 'https://github.com/Pankajvis1/FinalAssessment.git'
            }
        }

        stage('Build') {
            steps {
                dir('') {
                    bat 'mvn clean'
                }
            }
        }

        stage('Compile') {
            steps {
                dir('') {
                    bat 'mvn compile'
                }
            }
        }

        stage('Test') {
            steps {
                dir('') {
                    bat 'mvn test'
                }
            }
        }

        stage('Package') {
            steps {
                dir('') {
                    bat 'mvn package'
                }
            }
        }
    }

    post {
        success {
            echo 'Build Successful'
        }

        failure {
            echo 'Build Failed'
        }
    }
}