pipeline {
    agent any

    tools {
        maven 'Maven3'
        jdk 'jdk 17'
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Pankajvis1/FinalAssessment.git'
            }
        }

        stage('Build') {
            steps {
                dir('EcommerceAutomationFramework_FinalAssessment') {
                    bat 'mvn clean'
                }
            }
        }

        stage('Compile') {
            steps {
                dir('EcommerceAutomationFramework_FinalAssessment') {
                    bat 'mvn compile'
                }
            }
        }

        stage('Test') {
            steps {
                dir('EcommerceAutomationFramework_FinalAssessment') {
                    bat 'mvn test'
                }
            }
        }

        stage('Package') {
            steps {
                dir('EcommerceAutomationFramework_FinalAssessment') {
                    bat 'mvn package'
                }
            }
        }
    }

    post {
        success {
            echo 'Build Successful'
        }

        failure {
            echo 'Build Failed'
        }
    }
}
